#include <vector>
#include <cstdio>
/*
int sum[100010];
int target;

int main(){
	int k = 0;
	for (int i = 0; i < 4; ++i)
	{
		scanf("%d", &sum[i]);
	}

	scanf("%d", &target);
	for (int i = 0, j = 3; i < j;)
	{
	 	if(sum[i] + sum[j] == target) {
	 		printf("[%d, %d]\n", i, j);
	 		break;
	 	}
	 	else if(sum[i] + sum[j] < target) i++;
	 	else j--;
	}
}
*/
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        
    }
};